// Legacy compatibility module.
// Product modal logic now lives in ./product-modal.js.
// Merknad modal logic now lives in ./merknad-modal.js.

export function renderModalContent() {
  console.warn('renderModalContent() is deprecated. Use openProductModal() instead.')
}